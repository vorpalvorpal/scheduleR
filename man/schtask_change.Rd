% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change.R
\name{schtask_change}
\alias{schtask_change}
\title{Modify a scheduled task}
\usage{
schtask_change(
  task_name,
  task_run = NULL,
  start_time = NULL,
  end_time = NULL,
  duration = NULL,
  interval = NULL,
  start_date = NULL,
  end_date = NULL,
  run_level = NULL,
  run_as_user = NULL,
  run_as_password = NULL,
  enable = NULL,
  kill_on_end = FALSE,
  delete_when_done = FALSE,
  interactive_only = FALSE
)
}
\arguments{
\item{task_name}{Character string. Name of the task to modify. For tasks in
subfolders, include the full path (e.g., \code{"\\\\MyFolder\\\\MyTask"}).}

\item{task_run}{Character string. New program or command to run. Optional.}

\item{start_time}{Character string. New start time in HH:MM 24-hour format. Optional.}

\item{end_time}{Character string. New end time in HH:MM 24-hour format. Optional.}

\item{duration}{Character string. New maximum duration in HHHH:MM format. Optional.
Cannot be used with \code{end_time}.}

\item{interval}{Integer. New repetition interval in minutes (1-599940). Optional.}

\item{start_date}{Date or character string. New start date. Optional.}

\item{end_date}{Date or character string. New end date. Optional.}

\item{run_level}{Character string. New run level: \code{"LIMITED"} or \code{"HIGHEST"}. Optional.}

\item{run_as_user}{Character string. New user account to run the task.
If specified, you will be prompted for the password unless \code{run_as_user}
is \code{"SYSTEM"}. Optional.}

\item{run_as_password}{Character string. Password for the \code{run_as_user} account.
If \code{NULL} and \code{run_as_user} is specified (and not \code{"SYSTEM"}), you will be
prompted for the password. Optional.}

\item{enable}{Logical. If \code{TRUE}, enables the task. If \code{FALSE}, disables the task.
If \code{NULL} (default), the enabled state is not changed. Optional.}

\item{kill_on_end}{Logical. If \code{TRUE}, stops the program when end time or
duration is reached. Default is \code{FALSE}.}

\item{delete_when_done}{Logical. If \code{TRUE}, marks the task to be deleted
after completion. Default is \code{FALSE}.}

\item{interactive_only}{Logical. If \code{TRUE}, the task only runs when the
user is logged on. Default is \code{FALSE}.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
Changes properties of an existing scheduled task. Only specified parameters
are modified; all other properties remain unchanged.
}
\examples{
\dontrun{
# Change the program a task runs
schtask_change("MyTask", task_run = "C:\\\\NewPath\\\\newprogram.exe")

# Change the start time
schtask_change("MyTask", start_time = "08:30")

# Disable a task
schtask_change("MyTask", enable = FALSE)

# Change the user account (will prompt for password)
schtask_change("MyTask", run_as_user = "DOMAIN\\\\NewUser")

# Change to run as SYSTEM (no password required)
schtask_change("MyTask", run_as_user = "SYSTEM")
}
}
