% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{schtask_query}
\alias{schtask_query}
\title{Query scheduled tasks}
\usage{
schtask_query(task_name = NULL, verbose = TRUE)
}
\arguments{
\item{task_name}{Optional character string specifying a single task to query.
If \code{NULL} (default), all tasks are returned. Task names in subfolders should

include the full path (e.g., \code{"\\\\Microsoft\\\\Office\\\\Office Subscription Maintenance"}).}

\item{verbose}{Logical. If \code{TRUE} (default), returns detailed task information
including run times, last result, user info, etc. If \code{FALSE}, returns only
basic information (task name, next run time, status).}
}
\value{
A tibble containing task information. The columns depend on the \code{verbose}
parameter:

\strong{When \code{verbose = FALSE}:}
\itemize{
\item \code{task_name}: Name of the scheduled task
\item \code{next_run_time}: Next scheduled run time
\item \code{status}: Current status of the task
}

\strong{When \code{verbose = TRUE}:} Additional columns including \code{last_run_time},
\code{last_result}, \code{author}, \code{task_to_run}, \code{start_in}, \code{comment},
\code{scheduled_task_state}, \code{idle_time}, \code{power_management}, \code{run_as_user},

\code{delete_task_if_not_rescheduled}, \code{stop_task_if_runs_x_hours_and_x_mins},
\code{schedule}, \code{schedule_type}, \code{start_time}, \code{start_date}, \code{end_date},
\code{days}, \code{months}, \code{repeat_every}, \code{repeat_until_time}, \code{repeat_until_duration},
\code{repeat_stop_if_still_running}.
}
\description{
Retrieves information about scheduled tasks from Windows Task Scheduler.
}
\examples{
\dontrun{
# Query all tasks with verbose output
tasks <- schtask_query()

# Query all tasks with basic output
tasks <- schtask_query(verbose = FALSE)

# Query a specific task
task <- schtask_query(task_name = "MyBackupTask")

# Query a task in a subfolder
task <- schtask_query(task_name = "\\\\Microsoft\\\\Windows\\\\Defrag\\\\ScheduledDefrag")
}
}
