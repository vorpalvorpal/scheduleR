# Helper file to access internal functions for testing
# This file is automatically sourced by testthat

# Access internal functions from the package namespace
.validate_task_name <- scheduleR:::.validate_task_name
.validate_time <- scheduleR:::.validate_time
.validate_date <- scheduleR:::.validate_date
.quote_path <- scheduleR:::.quote_path
.quote_task_name <- scheduleR:::.quote_task_name
.normalise_day_of_week <- scheduleR:::.normalise_day_of_week
.normalise_days_of_week <- scheduleR:::.normalise_days_of_week
.normalise_months <- scheduleR:::.normalise_months
.build_create_args <- scheduleR:::.build_create_args
.to_snake_case_names <- scheduleR:::.to_snake_case_names
